export interface SkillList {
    skillName: string;
    skillValue: number;
}

export interface SkillRatio {
    HTML5: number;
    СSS3: number;
    JavaScript: number;
    React: number;
    TypeScript: number;
}
